"use client"

// /contact now serves the updated Contact page (same content as /contact-us),
// so both URLs show the current agentic-AI contact experience and form.
import ContactUsPage from "@/app/contact-us/page"

export default function ContactPage() {
  return <ContactUsPage />
}
